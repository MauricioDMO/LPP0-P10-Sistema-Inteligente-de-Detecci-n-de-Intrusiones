This directory is intentionally not mounted into Suricata anymore.

Runtime rule files are generated by `suricata-update` inside the Docker volume
`suricata-rules`. Local policy rules are stored in PostgreSQL and managed from
the Suricata management UI.
